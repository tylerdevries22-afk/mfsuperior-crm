import { FreightCollectionScreen } from "@/route-support/freight";
import { CONTRACTS_SPEC } from "@/features/freight-screen-specs";

export default function ServiceProgramsScreen() {
  return <FreightCollectionScreen spec={CONTRACTS_SPEC} />;
}
