import { EQUIPMENT_DETAIL_SPEC } from "@/features/freight-screen-specs";
import { FreightDetailScreen } from "@/route-support/freight";

export default function EquipmentMarketplaceDetailScreen() {
  return <FreightDetailScreen spec={EQUIPMENT_DETAIL_SPEC} />;
}
