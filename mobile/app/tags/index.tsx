import { FreightCollectionScreen } from "@/route-support/freight";
import { TAGS_SPEC } from "@/features/freight-screen-specs";

export default function TagsScreen() {
  return <FreightCollectionScreen spec={TAGS_SPEC} />;
}
